# Copiá este archivo a ".env" (sin el ".example") para probar Comentarios en local.
# ".env" ya está en .gitignore, así que nunca se sube al repo.

DATABASE_URL=postgres://usuario:password@localhost:5432/pfj_local
